<div class="container">
  <div class="row">
    <div class="col-12">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><%= @video.title %> - 受講状況分析</h1>
        <div>
          <%= link_to "管理者追加", video_management_add_manager_path(video_id: @video), class: "btn btn-outline-primary" %>
          <%= link_to "動画編集", edit_video_path(@video), class: "btn btn-primary" %>
          <%= link_to "戻る", root_path, class: "btn btn-secondary" %>
        </div>
      </div>

      <!-- CSVエクスポートボタン -->
      <div class="card mb-4">
        <div class="card-body">
          <h5 class="card-title">📊 データエクスポート</h5>
          <p class="text-muted mb-3">分析データをCSV形式でダウンロードできます（個別セッションは学習セッション履歴から）</p>
          <div class="row">
            <div class="col-md-6 mb-2">
              <%= link_to video_management_export_summary_path(video_id: @video, format: :csv), class: "btn btn-success w-100", data: { turbo: false } do %>
                <i class="bi bi-file-earmark-spreadsheet"></i> 全体サマリー
              <% end %>
              <small class="text-muted d-block mt-1">動画全体の統計情報</small>
            </div>
            <div class="col-md-6 mb-2">
              <%= link_to video_management_export_questions_path(video_id: @video, format: :csv), class: "btn btn-success w-100", data: { turbo: false } do %>
                <i class="bi bi-file-earmark-spreadsheet"></i> 問題別統計
              <% end %>
              <small class="text-muted d-block mt-1">各問題の回答統計</small>
            </div>
          </div>
        </div>
      </div>

      <!-- 全体統計 -->
      <div class="row mb-4">
        <div class="col-md-3">
          <div class="card text-center">
            <div class="card-body">
              <h5 class="card-title">総回答数</h5>
              <h2 class="text-primary"><%= @total_responses %></h2>
            </div>
          </div>
        </div>
        <div class="col-md-3">
          <div class="card text-center">
            <div class="card-body">
              <h5 class="card-title">受講者数</h5>
              <h2 class="text-info"><%= @unique_users %></h2>
            </div>
          </div>
        </div>
        <div class="col-md-3">
          <div class="card text-center">
            <div class="card-body">
              <h5 class="card-title">正解数</h5>
              <h2 class="text-success"><%= @correct_responses %></h2>
            </div>
          </div>
        </div>
        <div class="col-md-3">
          <div class="card text-center">
            <div class="card-body">
              <h5 class="card-title">正答率</h5>
              <h2 class="text-warning"><%= @accuracy_rate %>%</h2>
            </div>
          </div>
        </div>
      </div>

      <!-- 問題別統計 -->
      <div class="card mb-4">
        <div class="card-header">
          <h3>問題別統計</h3>
        </div>
        <div class="card-body">
          <% if @question_stats.any? %>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th>問題</th>
                    <th>タイプ</th>
                    <th>時間位置</th>
                    <th>回答数</th>
                    <th>正解数</th>
                    <th>正答率</th>
                    <th>平均回答時間</th>
                  </tr>
                </thead>
                <tbody>
                  <% @question_stats.each do |stat| %>
                    <tr>
                      <td><%= truncate(stat[:question].content, length: 50) %></td>
                      <td>
                        <span class="badge <%= question_type_badge_class(stat[:question].question_type) %>">
                          <%= question_type_display(stat[:question].question_type) %>
                        </span>
                      </td>
                      <td><%= format_time_position(stat[:question].time_position) %></td>
                      <td><%= stat[:total_responses] %></td>
                      <td><%= stat[:correct_responses] %></td>
                      <td>
                        <% if stat[:accuracy_rate] >= 80 %>
                          <span class="text-success"><%= stat[:accuracy_rate] %>%</span>
                        <% elsif stat[:accuracy_rate] >= 60 %>
                          <span class="text-warning"><%= stat[:accuracy_rate] %>%</span>
                        <% else %>
                          <span class="text-danger"><%= stat[:accuracy_rate] %>%</span>
                        <% end %>
                      </td>
                      <td><%= stat[:average_response_time] %>秒</td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <% else %>
            <p class="text-muted">この動画には問題がありません。</p>
          <% end %>
        </div>
      </div>

      <!-- タイムラインデータ可視化グラフ -->
      <% if @learning_sessions.any? %>
        <div class="card mb-4">
          <div class="card-header d-flex justify-content-between align-items-center">
            <h3 class="mb-0">学習タイムライン分析グラフ</h3>
            <small class="text-muted">💡 マウスホイールでズーム、ドラッグで移動、ダブルクリックでリセット</small>
          </div>
          <div class="card-body">
            <!-- グラフナビゲーション -->
            <ul class="nav nav-tabs mb-3" id="chartTabs" role="tablist">
              <li class="nav-item" role="presentation">
                <button class="nav-link active" id="score-timeline-tab" data-bs-toggle="tab" data-bs-target="#score-timeline" type="button">
                  集中スコア推移
                </button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="event-distribution-tab" data-bs-toggle="tab" data-bs-target="#event-distribution" type="button">
                  イベント分布
                </button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="hourly-activity-tab" data-bs-toggle="tab" data-bs-target="#hourly-activity" type="button">
                  時間帯別活動
                </button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="user-scores-tab" data-bs-toggle="tab" data-bs-target="#user-scores" type="button">
                  ユーザー別スコア
                </button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="video-density-tab" data-bs-toggle="tab" data-bs-target="#video-density" type="button">
                  動画イベント密度
                </button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="video-operations-tab" data-bs-toggle="tab" data-bs-target="#video-operations" type="button">
                  動画操作マップ
                </button>
              </li>
              <li class="nav-item" role="presentation">
                <button class="nav-link" id="user-operations-tab" data-bs-toggle="tab" data-bs-target="#user-operations" type="button">
                  ユーザー別操作
                </button>
              </li>
            </ul>

            <!-- グラフコンテンツ -->
            <div class="tab-content" id="chartTabsContent">
              <!-- 1. 集中スコア推移グラフ -->
              <div class="tab-pane fade show active" id="score-timeline" role="tabpanel">
                <div class="mb-3">
                  <div class="d-flex justify-content-between align-items-center mb-2">
                    <button class="btn btn-sm btn-outline-info" type="button" data-bs-toggle="collapse" data-bs-target="#scoreTimelineTogglesCollapse" aria-expanded="true">
                      <i class="bi bi-chevron-down"></i> 表示するセッション
                    </button>
                    <div>
                      <button class="btn btn-sm btn-outline-primary" onclick="toggleAllSessions('scoreTimeline', true)">すべて表示</button>
                      <button class="btn btn-sm btn-outline-secondary" onclick="toggleAllSessions('scoreTimeline', false)">すべて非表示</button>
                    </div>
                  </div>
                  <div class="collapse show" id="scoreTimelineTogglesCollapse">
                    <div id="scoreTimelineSessionToggles" class="row"></div>
                  </div>
                  
                  <!-- 動画時刻0のデータフィルター -->
                  <div class="mt-3 mb-3">
                    <div class="form-check">
                      <input class="form-check-input" type="checkbox" id="hideVideoTime0" onchange="toggleVideoTime0Filter()">
                      <label class="form-check-label" for="hideVideoTime0">
                        動画時刻0秒のデータを非表示にする
                      </label>
                      <small class="text-muted d-block ms-4">動画が再生される前のデータポイントを除外します</small>
                    </div>
                  </div>
                </div>
                <div class="chart-container" style="position: relative; height: 800px;">
                  <canvas id="scoreTimelineChart"></canvas>
                </div>
                <p class="text-muted mt-2 text-center">
                  <small>各セッションの集中スコアの時間的推移を表示します。セッション経過時間（秒）に対するスコアの変化を確認できます。</small>
                </p>
              </div>

              <!-- 2. イベントタイプ分布グラフ -->
              <div class="tab-pane fade" id="event-distribution" role="tabpanel">
                <div class="row">
                  <div class="col-md-6 offset-md-3">
                    <div class="chart-container" style="position: relative; height: 400px;">
                      <canvas id="eventDistributionChart"></canvas>
                    </div>
                  </div>
                </div>
                <p class="text-muted mt-2 text-center">
                  <small>全セッションで記録されたイベントのカテゴリ別分布を表示します。</small>
                </p>
              </div>

              <!-- 3. 時間帯別活動量グラフ -->
              <div class="tab-pane fade" id="hourly-activity" role="tabpanel">
                <div class="chart-container" style="position: relative; height: 400px;">
                  <canvas id="hourlyActivityChart"></canvas>
                </div>
                <p class="text-muted mt-2 text-center">
                  <small>1日の中でどの時間帯に学習活動が活発かを表示します。</small>
                </p>
              </div>

              <!-- 4. ユーザー別平均スコアグラフ -->
              <div class="tab-pane fade" id="user-scores" role="tabpanel">
                <div class="chart-container" style="position: relative; height: 400px;">
                  <canvas id="userScoresChart"></canvas>
                </div>
                <p class="text-muted mt-2 text-center">
                  <small>各ユーザーの平均集中スコアを比較表示します。</small>
                </p>
              </div>

              <!-- 5. 動画時間別イベント密度グラフ -->
              <div class="tab-pane fade" id="video-density" role="tabpanel">
                <div class="chart-container" style="position: relative; height: 400px;">
                  <canvas id="videoDensityChart"></canvas>
                </div>
                <p class="text-muted mt-2 text-center">
                  <small>動画のどの部分で学習活動が活発かを30秒単位で表示します。</small>
                </p>
              </div>

              <!-- 6. 動画操作マップ（全体） -->
              <div class="tab-pane fade" id="video-operations" role="tabpanel">
                <div class="chart-container" style="position: relative; height: 400px;">
                  <canvas id="videoOperationsChart"></canvas>
                </div>
                <p class="text-muted mt-2 text-center">
                  <small>動画内のどの時間で一時停止やスキップ（シーク）が行われたかを表示します。棒が高いほど操作が多い箇所です。</small>
                </p>
              </div>

              <!-- 7. ユーザー別動画操作 -->
              <div class="tab-pane fade" id="user-operations" role="tabpanel">
                <div class="mb-3">
                  <div class="d-flex justify-content-between align-items-center mb-2">
                    <button class="btn btn-sm btn-outline-info" type="button" data-bs-toggle="collapse" data-bs-target="#userOperationsTogglesCollapse" aria-expanded="true">
                      <i class="bi bi-chevron-down"></i> 表示するセッション
                    </button>
                    <div>
                      <button class="btn btn-sm btn-outline-primary" onclick="toggleAllSessions('userOperations', true)">すべて表示</button>
                      <button class="btn btn-sm btn-outline-secondary" onclick="toggleAllSessions('userOperations', false)">すべて非表示</button>
                    </div>
                  </div>
                  <div class="collapse show" id="userOperationsTogglesCollapse">
                    <div id="userOperationsSessionToggles" class="row"></div>
                  </div>
                </div>
                <div class="chart-container" style="position: relative; height: 400px;">
                  <canvas id="userOperationsChart"></canvas>
                </div>
                <p class="text-muted mt-2 text-center">
                  <small>各ユーザーが動画のどの時間で操作を行ったかをセッション別に表示します。</small>
                </p>
              </div>
            </div>
          </div>
        </div>
      <% end %>

      <!-- 学習セッション統計 -->
      <div class="row mb-4">
        <div class="col-md-3">
          <div class="card text-center">
            <div class="card-body">
              <h5 class="card-title">総学習セッション</h5>
              <h2 class="text-primary"><%= @total_sessions %></h2>
            </div>
          </div>
        </div>
        <div class="col-md-3">
          <div class="card text-center">
            <div class="card-body">
              <h5 class="card-title">完了セッション</h5>
              <h2 class="text-success"><%= @completed_sessions %></h2>
            </div>
          </div>
        </div>
        <div class="col-md-3">
          <div class="card text-center">
            <div class="card-body">
              <h5 class="card-title">平均学習時間</h5>
              <h2 class="text-info">
                <% if @average_session_duration && @average_session_duration > 0 %>
                  <%= (@average_session_duration / 60).round(1) %>分
                <% else %>
                  -
                <% end %>
              </h2>
            </div>
          </div>
        </div>
        <div class="col-md-3">
          <div class="card text-center">
            <div class="card-body">
              <h5 class="card-title">平均最終スコア</h5>
              <h2 class="text-warning">
                <% if @average_final_score %>
                  <%= @average_final_score.round(1) %>点
                <% else %>
                  -
                <% end %>
              </h2>
            </div>
          </div>
        </div>
      </div>

      <!-- 動画操作統計（1分以上のセッション平均） -->
      <div class="card mb-4">
        <div class="card-header">
          <h3>動画操作統計 <small class="text-muted">(1分以上のセッション平均)</small></h3>
        </div>
        <div class="card-body">
          <div class="row">
            <div class="col-md-3">
              <div class="card text-center">
                <div class="card-body">
                  <h5 class="card-title">⏸️ 一時停止</h5>
                  <h2 class="text-warning"><%= @avg_pause_count %></h2>
                  <p class="text-muted">回/セッション</p>
                </div>
              </div>
            </div>
            <div class="col-md-3">
              <div class="card text-center">
                <div class="card-body">
                  <h5 class="card-title">⏩ 前進スキップ</h5>
                  <h2 class="text-primary"><%= @avg_forward_seek_count %></h2>
                  <p class="text-muted">回/セッション</p>
                </div>
              </div>
            </div>
            <div class="col-md-3">
              <div class="card text-center">
                <div class="card-body">
                  <h5 class="card-title">⏪ 後退スキップ</h5>
                  <h2 class="text-info"><%= @avg_backward_seek_count %></h2>
                  <p class="text-muted">回/セッション</p>
                </div>
              </div>
            </div>
            <div class="col-md-3">
              <div class="card text-center">
                <div class="card-body">
                  <h5 class="card-title">⚡ 再生速度変更</h5>
                  <h2 class="text-success"><%= @avg_playback_rate_change_count %></h2>
                  <p class="text-muted">回/セッション</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- 学習セッション一覧 -->
      <div class="card mb-4">
        <div class="card-header">
          <h3>学習セッション履歴</h3>
        </div>
        <div class="card-body">
          <% if @learning_sessions.any? %>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                  <tr>
                    <th>受講者</th>
                    <th>開始時刻</th>
                    <th>終了時刻</th>
                    <th>学習時間</th>
                    <th>最終スコア</th>
                    <th>イベント数</th>
                    <th title="一時停止回数">⏸️</th>
                    <th title="前進スキップ回数">⏩</th>
                    <th title="後退スキップ回数">⏪</th>
                    <th title="再生速度変更回数">⚡</th>
                    <th>エクスポート</th>
                    <th>詳細</th>
                    <th>操作</th>
                  </tr>
                </thead>
                <tbody>
                  <% @learning_sessions.each do |session| %>
                    <% operation_stats = session.video_operation_stats %>
                    <tr>
                      <td>
                        <%= session.user.name %>
                        <br><small class="text-muted"><%= session.user.email %></small>
                      </td>
                      <td><%= session.session_start_time.strftime("%Y/%m/%d %H:%M") %></td>
                      <td>
                        <% if session.session_end_time %>
                          <%= session.session_end_time.strftime("%Y/%m/%d %H:%M") %>
                        <% else %>
                          <span class="text-warning">進行中</span>
                        <% end %>
                      </td>
                      <td><%= session.format_duration %></td>
                      <td>
                        <% if session.final_score %>
                          <span class="badge <%= session.final_score >= 70 ? 'bg-success' : session.final_score >= 50 ? 'bg-warning' : 'bg-danger' %>">
                            <%= session.final_score.round(1) %>点
                          </span>
                        <% else %>
                          -
                        <% end %>
                      </td>
                      <td><%= session.total_events || session.timestamp_events.count %></td>
                      <td class="text-center"><%= operation_stats[:pause_count] %></td>
                      <td class="text-center"><%= operation_stats[:forward_seek_count] %></td>
                      <td class="text-center"><%= operation_stats[:backward_seek_count] %></td>
                      <td class="text-center"><%= operation_stats[:playback_rate_change_count] %></td>
                      <td>
                        <div class="btn-group-vertical btn-group-sm">
                          <%= link_to "📊", video_management_export_session_detail_path(video_id: @video, session_id: session.id, format: :csv), 
                              class: "btn btn-sm btn-outline-success", 
                              title: "セッション詳細CSV",
                              data: { turbo: false } %>
                          <%= link_to "📝", video_management_export_session_events_path(video_id: @video, session_id: session.id, format: :csv), 
                              class: "btn btn-sm btn-outline-success", 
                              title: "イベントタイムラインCSV",
                              data: { turbo: false } %>
                        </div>
                      </td>
                      <td>
                        <button class="btn btn-sm btn-primary" onclick="showSessionDetail(<%= session.id %>)">
                          タイムライン表示
                        </button>
                      </td>
                      <td>
                        <%= button_to "削除", 
                            video_management_destroy_session_path(video_id: @video, session_id: session.id), 
                            method: :delete, 
                            class: "btn btn-sm btn-danger",
                            data: { 
                              turbo_confirm: "本当にこのセッション（#{session.user.name}、#{session.session_start_time.strftime('%Y/%m/%d %H:%M')}）を削除しますか？関連するすべてのイベントデータも削除されます。" 
                            },
                            form: { style: "display: inline-block;" } %>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <% else %>
            <p class="text-muted">まだ学習セッションがありません。</p>
          <% end %>
        </div>
      </div>

      <!-- 詳細な回答履歴 -->
      <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
          <h3 class="mb-0">回答履歴</h3>
          <button class="btn btn-danger btn-sm" onclick="deleteSelectedResponses()">
            <i class="bi bi-trash"></i> 選択した回答を削除
          </button>
        </div>
        <div class="card-body">
          <% if @user_responses.any? %>
            <div class="table-responsive">
              <table class="table table-striped table-sm">
                <thead>
                  <tr>
                    <th>
                      <input type="checkbox" id="selectAllResponses" onchange="toggleAllResponses(this.checked)">
                    </th>
                    <th>受講者</th>
                    <th>問題</th>
                    <th>回答</th>
                    <th>正誤</th>
                    <th>回答時間</th>
                    <th>回答日時</th>
                  </tr>
                </thead>
                <tbody>
                  <% @user_responses.each do |response| %>
                    <tr id="response-<%= response.id %>">
                      <td>
                        <input type="checkbox" class="response-checkbox" value="<%= response.id %>">
                      </td>
                      <td>
                        <% if response.user %>
                          <%= response.user.name %>
                          <br><small class="text-muted"><%= response.user.email %></small>
                        <% else %>
                          <em>ゲストユーザー</em>
                        <% end %>
                      </td>
                      <td><%= truncate(response.question.content, length: 40) %></td>
                      <td><%= truncate(response.user_answer, length: 30) %></td>
                      <td>
                        <% if response.is_correct %>
                          <span class="badge bg-success">正解</span>
                        <% else %>
                          <span class="badge bg-danger">不正解</span>
                        <% end %>
                      </td>
                      <td>
                        <% if response.response_time %>
                          <%= (response.response_time / 1000.0).round(1) %>秒
                        <% else %>
                          -
                        <% end %>
                      </td>
                      <td><%= response.created_at.strftime("%Y/%m/%d %H:%M") %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          <% else %>
            <p class="text-muted">まだ回答がありません。</p>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- セッション詳細モーダル -->
<div class="modal fade" id="sessionDetailModal" tabindex="-1">
  <div class="modal-dialog modal-xl">
    <div class="modal-content">
      <div class="modal-header">
        <div>
          <h5 class="modal-title">学習セッション詳細タイムライン</h5>
          <small class="text-muted">💡 マウスホイールでズーム、ドラッグで移動、ダブルクリックでリセット</small>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        <!-- グラフエリア -->
        <div id="chartContainer" style="position: relative; height: 600px; margin-bottom: 20px;">
          <canvas id="timelineChart"></canvas>
        </div>
        
        <!-- タイムスタンプログ -->
        <div class="mt-4">
          <h6>詳細イベントログ</h6>
          <div id="timestampLog" style="max-height: 300px; overflow-y: auto;">
            <!-- JavaScriptで動的に生成 -->
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="https://cdn.jsdelivr.net/npm/hammerjs@2.0.8"></script>
<script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-zoom@2.0.1/dist/chartjs-plugin-zoom.min.js"></script>
<style>
  .timestamp-entry {
    padding: 8px;
    margin-bottom: 4px;
    border-left: 3px solid #007bff;
    background-color: #f8f9fa;
    border-radius: 4px;
  }
  
  .timestamp-entry.score-event {
    border-left-color: #28a745;
  }
  
  .timestamp-entry.video-event {
    border-left-color: #ffc107;
  }
  
  .timestamp-entry.interaction-event {
    border-left-color: #17a2b8;
  }
  
  .timestamp-header {
    display: flex;
    justify-content: space-between;
    font-weight: bold;
    margin-bottom: 4px;
  }
  
  .timestamp-description {
    font-size: 0.9em;
    color: #6c757d;
  }
  
  /* トグルボタンのアイコン回転アニメーション */
  [data-bs-toggle="collapse"] i.bi-chevron-down {
    transition: transform 0.3s ease;
  }
  
  [data-bs-toggle="collapse"][aria-expanded="false"] i.bi-chevron-down {
    transform: rotate(-90deg);
  }
</style>

<script>
// グローバルスコープ汚染を避けるための即時実行関数
(function() {
  'use strict';
  
  // トグルボタンのアイコン回転制御
  document.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('[data-bs-toggle="collapse"]').forEach(button => {
      const targetId = button.getAttribute('data-bs-target');
      const target = document.querySelector(targetId);
      
      if (target) {
        target.addEventListener('show.bs.collapse', function() {
          button.setAttribute('aria-expanded', 'true');
        });
        
        target.addEventListener('hide.bs.collapse', function() {
          button.setAttribute('aria-expanded', 'false');
        });
      }
    });
  });
  
  let sessionDetailChart = null;

  window.showSessionDetail = function(sessionId) {
    // モーダルを表示
    const modalElement = document.getElementById('sessionDetailModal');
    const modal = new bootstrap.Modal(modalElement);
    
    // モーダルが完全に表示された後にデータを取得
    modalElement.addEventListener('shown.bs.modal', function onShown() {
      // イベントリスナーを一度だけ実行
      modalElement.removeEventListener('shown.bs.modal', onShown);
      
      // セッションデータを取得
      const url = '<%= video_management_session_detail_path(video_id: @video, session_id: 'PLACEHOLDER') %>'.replace('PLACEHOLDER', sessionId);
      console.log('Fetching session data from:', url);
      
      fetch(url, {
        headers: {
          'X-Requested-With': 'XMLHttpRequest',
          'Accept': 'application/json'
        }
      })
      .then(response => {
        console.log('Response status:', response.status);
        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }
        return response.json();
      })
      .then(data => {
        console.log('セッションデータ:', data);
        renderChart(data);
        renderTimestampLog(data);
      })
      .catch(error => {
        console.error('セッションデータ取得エラー:', error);
        alert('セッションデータの取得に失敗しました: ' + error.message);
      });
    });
    
    modal.show();
  };

  function renderChart(data) {
    const ctx = document.getElementById('timelineChart');
    if (!ctx) {
      console.error('timelineChart canvas が見つかりません');
      return;
    }
    
    console.log('グラフ描画開始:', data);
    
    // データが空の場合
    if (!data.timeline || data.timeline.length === 0) {
      console.warn('タイムラインデータが空です');
      ctx.parentElement.innerHTML = '<p class="text-muted text-center">このセッションにはタイムラインデータがありません</p>';
      return;
    }
    
    // 既存のチャートを削除
    if (sessionDetailChart) {
      sessionDetailChart.destroy();
    }
    
    try {
      // 集中度スコアで色を決定する関数
      const getColorByScore = (score) => {
        if (score <= 25) return '#dc3545'; // 赤
        if (score <= 50) return '#fd7e14'; // オレンジ
        if (score <= 75) return '#ffc107'; // 黄色
        return '#28a745'; // 緑
      };
      
      // タイムラインデータを時系列順に並べ、各点に色情報を追加
      const timelineData = data.timeline.map(e => ({
        x: e.x,
        y: e.video_time || 0,
        score: e.y,
        description: e.description,
        color: getColorByScore(e.y)
      }));
      
      // 動画操作イベントをスキップと停止に分類
      const skipOpsData = (data.video_operations || []).filter(op => 
        op.operation.includes('skip') || op.operation.includes('seek') || op.description.includes('スキップ') || op.description.includes('シーク')
      ).map(op => ({
        x: op.time,
        y: op.video_time || 0,
        description: op.description,
        operation: op.operation
      }));
      
      const pauseOpsData = (data.video_operations || []).filter(op => 
        op.operation.includes('pause') || op.operation.includes('stop') || op.description.includes('停止') || op.description.includes('一時停止')
      ).map(op => ({
        x: op.time,
        y: op.video_time || 0,
        description: op.description,
        operation: op.operation
      }));
      
      // その他の動画操作
      const otherOpsData = (data.video_operations || []).filter(op => 
        !op.operation.includes('skip') && !op.operation.includes('seek') && 
        !op.operation.includes('pause') && !op.operation.includes('stop') &&
        !op.description.includes('スキップ') && !op.description.includes('シーク') &&
        !op.description.includes('停止') && !op.description.includes('一時停止')
      ).map(op => ({
        x: op.time,
        y: op.video_time || 0,
        description: op.description,
        operation: op.operation
      }));
      
      // ユーザーインタラクション（問題回答、メモなど）
      const interactionsData = (data.interactions || []).map(i => ({
        x: i.time,
        y: i.video_time || 0,
        description: i.description,
        type: i.type
      }));
      
      const datasets = [
        // 凡例用のダミーデータセット（最初に配置して凡例の順序を制御）
        { label: '集中度: 0-25 (低)', data: [], borderColor: '#dc3545', backgroundColor: '#dc3545', pointRadius: 5 },
        { label: '集中度: 26-50', data: [], borderColor: '#fd7e14', backgroundColor: '#fd7e14', pointRadius: 5 },
        { label: '集中度: 51-75', data: [], borderColor: '#ffc107', backgroundColor: '#ffc107', pointRadius: 5 },
        { label: '集中度: 76-100 (高)', data: [], borderColor: '#28a745', backgroundColor: '#28a745', pointRadius: 5 },
        {
          label: '視聴タイムライン',
          data: timelineData,
          borderColor: '#6c757d',
          borderWidth: 2,
          showLine: true,
          tension: 0,
          pointRadius: 4,
          pointHoverRadius: 7,
          pointBackgroundColor: timelineData.map(d => d.color),
          pointBorderColor: timelineData.map(d => d.color)
        },
        {
          label: 'スキップ/シーク',
          data: skipOpsData,
          borderColor: '#6f42c1',
          backgroundColor: '#6f42c1',
          pointRadius: 10,
          pointHoverRadius: 13,
          pointStyle: 'triangle',
          showLine: false
        },
        {
          label: '一時停止',
          data: pauseOpsData,
          borderColor: '#6f42c1',
          backgroundColor: '#6f42c1',
          pointRadius: 10,
          pointHoverRadius: 13,
          pointStyle: 'rect',
          showLine: false
        },
        {
          label: 'その他の操作',
          data: otherOpsData,
          borderColor: '#6f42c1',
          backgroundColor: '#6f42c1',
          pointRadius: 9,
          pointHoverRadius: 12,
          pointStyle: 'circle',
          showLine: false
        },
        {
          label: 'ユーザー操作',
          data: interactionsData,
          borderColor: '#20c997',
          backgroundColor: '#20c997',
          pointRadius: 9,
          pointHoverRadius: 12,
          pointStyle: 'star',
          showLine: false
        }
      ];
      
      sessionDetailChart = new Chart(ctx, {
      type: 'scatter',
      data: { datasets },
      options: {
        responsive: true,
        maintainAspectRatio: false,
        scales: {
          x: {
            type: 'linear',
            title: {
              display: true,
              text: 'セッション経過時間（秒）'
            },
            ticks: {
              callback: function(value) {
                const minutes = Math.floor(value / 60);
                const seconds = Math.floor(value % 60);
                return `${minutes}:${seconds.toString().padStart(2, '0')}`;
              }
            }
          },
          y: {
            type: 'linear',
            title: {
              display: true,
              text: '動画再生時間（秒）'
            },
            ticks: {
              callback: function(value) {
                const minutes = Math.floor(value / 60);
                const seconds = Math.floor(value % 60);
                return `${minutes}:${seconds.toString().padStart(2, '0')}`;
              }
            },
            beginAtZero: true
          }
        },
        plugins: {
          legend: {
            display: true,
            position: 'top'
          },
          zoom: {
            zoom: {
              wheel: {
                enabled: true,
              },
              pinch: {
                enabled: true
              },
              mode: 'xy',
            },
            pan: {
              enabled: true,
              mode: 'xy',
            },
            limits: {
              y: {min: 0, max: 100}
            }
          },
          tooltip: {
            callbacks: {
              label: function(context) {
                const rawData = context.raw;
                
                // イベントマーカーの場合（datasetIndexが5以上）
                if (context.datasetIndex >= 5) {
                  const videoMinutes = Math.floor(context.parsed.y / 60);
                  const videoSeconds = Math.floor(context.parsed.y % 60);
                  return `${rawData.description} | 動画: ${videoMinutes}:${videoSeconds.toString().padStart(2, '0')}`;
                }
                
                // 通常のタイムラインポイント
                const videoMinutes = Math.floor(context.parsed.y / 60);
                const videoSeconds = Math.floor(context.parsed.y % 60);
                return `集中度: ${rawData.score.toFixed(1)}点 | 動画: ${videoMinutes}:${videoSeconds.toString().padStart(2, '0')}`;
              },
              title: function(context) {
                const minutes = Math.floor(context[0].parsed.x / 60);
                const seconds = Math.floor(context[0].parsed.x % 60);
                return `経過時間: ${minutes}:${seconds.toString().padStart(2, '0')}`;
              }
            }
          }
        }
      }
    });
    
    console.log('グラフ作成完了:', sessionDetailChart);
    
    // ダブルクリックでズームリセット
    ctx.ondblclick = function() {
      sessionDetailChart.resetZoom();
    };
    } catch (error) {
      console.error('グラフ作成エラー:', error);
      ctx.parentElement.innerHTML = '<p class="text-danger text-center">グラフの作成に失敗しました: ' + error.message + '</p>';
    }
  }

  function renderTimestampLog(data) {
    const logContainer = document.getElementById('timestampLog');
    
    // 全イベントを時系列順に結合
    const allEvents = [
      ...data.timeline.map(e => ({...e, category: 'timeline'})),
      ...data.score_changes.map(e => ({...e, category: 'score'})),
      ...data.video_operations.map(e => ({...e, category: 'video'})),
      ...data.interactions.map(e => ({...e, category: 'interaction'}))
    ].sort((a, b) => (a.time || a.x) - (b.time || b.x));
    
    logContainer.innerHTML = allEvents.map(event => {
      const time = event.time || event.x;
      const category = event.category;
      
      return `
        <div class="timestamp-entry ${category}-event">
          <div class="timestamp-header">
            <span>${formatTime(time)}</span>
            <span class="badge bg-secondary">${category}</span>
          </div>
          <div class="timestamp-description">
            ${event.description || event.reason || event.operation || event.type}
          </div>
        </div>
      `;
    }).join('');
  }

  function formatTime(seconds) {
    const minutes = Math.floor(seconds / 60);
    const secs = Math.floor(seconds % 60);
    return `${minutes}:${secs.toString().padStart(2, '0')}`;
  }
  
  // 個別の回答を削除する関数
  window.deleteResponse = function(responseId) {
    if (!confirm('この回答を削除してもよろしいですか？\nこの操作は取り消せません。')) {
      return;
    }
    
    const token = document.querySelector('meta[name="csrf-token"]').content;
    
    fetch('/video_management/delete_response', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-CSRF-Token': token
      },
      body: JSON.stringify({ response_id: responseId })
    })
    .then(response => response.json())
    .then(data => {
      if (data.success) {
        // テーブル行を削除
        const row = document.getElementById('response-' + responseId);
        if (row) {
          row.remove();
        }
        alert('回答を削除しました。');
      } else {
        alert('回答の削除に失敗しました: ' + (data.error || '不明なエラー'));
      }
    })
    .catch(error => {
      console.error('削除エラー:', error);
      alert('回答の削除中にエラーが発生しました。');
    });
  };
  
  // 全選択/全解除
  window.toggleAllResponses = function(checked) {
    document.querySelectorAll('.response-checkbox').forEach(checkbox => {
      checkbox.checked = checked;
    });
  };
  
  // 選択した回答をまとめて削除
  window.deleteSelectedResponses = function() {
    const checkboxes = document.querySelectorAll('.response-checkbox:checked');
    const responseIds = Array.from(checkboxes).map(cb => parseInt(cb.value));
    
    if (responseIds.length === 0) {
      alert('削除する回答を選択してください。');
      return;
    }
    
    if (!confirm(`選択した${responseIds.length}件の回答を削除してもよろしいですか？\nこの操作は取り消せません。`)) {
      return;
    }
    
    const token = document.querySelector('meta[name="csrf-token"]').content;
    
    fetch('/video_management/delete_responses', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-CSRF-Token': token
      },
      body: JSON.stringify({ response_ids: responseIds })
    })
    .then(response => response.json())
    .then(data => {
      if (data.success) {
        // 削除された回答の行を削除
        responseIds.forEach(id => {
          const row = document.getElementById('response-' + id);
          if (row) {
            row.remove();
          }
        });
        // 全選択チェックボックスをリセット
        document.getElementById('selectAllResponses').checked = false;
        alert(`${data.deleted_count}件の回答を削除しました。`);
      } else {
        alert('回答の削除に失敗しました: ' + (data.error || '不明なエラー'));
      }
    })
    .catch(error => {
      console.error('削除エラー:', error);
      alert('回答の削除中にエラーが発生しました。');
    });
  };
})();

// タイムライン分析グラフの初期化
<% if @learning_sessions.any? %>
// Turbo対応: DOMContentLoadedとturbo:loadの両方に対応
// チャートインスタンスを保持するグローバル変数
let chartInstances = {};

// セッション表示/非表示切り替え機能
function toggleAllSessions(chartType, show) {
  const chart = chartInstances[chartType];
  if (!chart) return;
  
  chart.data.datasets.forEach((dataset, index) => {
    const meta = chart.getDatasetMeta(index);
    meta.hidden = !show;
  });
  chart.update();
  
  // チェックボックスも更新
  const container = document.getElementById(chartType + 'SessionToggles');
  if (container) {
    container.querySelectorAll('input[type="checkbox"]').forEach(checkbox => {
      checkbox.checked = show;
    });
  }
}

function toggleSession(chartType, sessionId) {
  const chart = chartInstances[chartType];
  if (!chart) return;
  
  // このセッションに属する全てのデータセット（スコアライン＋操作マーカー）を切り替え
  let newHiddenState = null;
  
  chart.data.datasets.forEach((dataset, index) => {
    if (dataset.sessionId === sessionId) {
      const meta = chart.getDatasetMeta(index);
      // 最初のデータセットの状態を基準に切り替え
      if (newHiddenState === null) {
        newHiddenState = !meta.hidden;
      }
      meta.hidden = newHiddenState;
    }
  });
  
  chart.update();
}

function deleteSelectedSessions(chartType) {
  // チェックされているセッションIDを取得
  const checkboxes = document.querySelectorAll(`input[id^="${chartType}_session_"]:checked`);
  const sessionIds = Array.from(checkboxes).map(cb => {
    const match = cb.id.match(/session_(\d+)$/);
    return match ? parseInt(match[1]) : null;
  }).filter(id => id !== null);
  
  if (sessionIds.length === 0) {
    alert('削除するセッションを選択してください。');
    return;
  }
  
  if (!confirm(`選択した${sessionIds.length}件のセッションを削除してもよろしいですか？\nこの操作は取り消せません。`)) {
    return;
  }
  
  // CSRFトークンを取得
  const token = document.querySelector('meta[name="csrf-token"]').content;
  
  // バックエンドに削除リクエストを送信
  fetch('/video_management/delete_sessions', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'X-CSRF-Token': token
    },
    body: JSON.stringify({ session_ids: sessionIds })
  })
  .then(response => response.json())
  .then(data => {
    if (data.success) {
      let message = `${data.deleted_count}件のセッションを削除しました。`;
      if (data.deleted_responses_count > 0) {
        message += `\n関連する${data.deleted_responses_count}件の回答履歴も削除されました。`;
      }
      alert(message);
      // ページをリロードして更新
      window.location.reload();
    } else {
      alert('セッションの削除に失敗しました: ' + (data.error || '不明なエラー'));
    }
  })
  .catch(error => {
    console.error('削除エラー:', error);
    alert('セッションの削除中にエラーが発生しました。');
  });
}

function toggleOperationMarkers(chartType, show) {
  const chart = chartInstances[chartType];
  if (!chart) return;
  
  // 操作マーカーのデータセットのみを切り替え（ラベルに「操作」を含むもの）
  chart.data.datasets.forEach((dataset, index) => {
    if (dataset.label && dataset.label.includes('操作')) {
      const meta = chart.getDatasetMeta(index);
      meta.hidden = !show;
    }
  });
  
  chart.update();
}

function toggleResponseMarkers(chartType, show) {
  const chart = chartInstances[chartType];
  if (!chart) return;
  
  // 反応速度マーカーのデータセットのみを切り替え（ラベルに「反応速度」を含むもの）
  chart.data.datasets.forEach((dataset, index) => {
    if (dataset.label && dataset.label.includes('反応速度')) {
      const meta = chart.getDatasetMeta(index);
      meta.hidden = !show;
    }
  });
  
  chart.update();
}

function createSessionToggles(chartType, datasets) {
  const container = document.getElementById(chartType + 'SessionToggles');
  if (!container) return;
  
  // 操作マーカーと反応速度マーカーの表示/非表示トグルを先頭に配置
  let html = `
    <div class="col-12 mb-3">
      <div class="card bg-light">
        <div class="card-body py-2">
          <div class="d-flex justify-content-between align-items-center mb-2">
            <div>
              <div class="form-check mb-2">
                <input class="form-check-input" type="checkbox" id="${chartType}_operation_markers" 
                       checked onchange="toggleOperationMarkers('${chartType}', this.checked)">
                <label class="form-check-label" for="${chartType}_operation_markers">
                  <span style="color: #6f42c1; font-size: 1.2em;">●</span> <strong>操作マーカー（紫）を表示</strong>
                </label>
              </div>
              <div class="form-check">
                <input class="form-check-input" type="checkbox" id="${chartType}_response_markers" 
                       checked onchange="toggleResponseMarkers('${chartType}', this.checked)">
                <label class="form-check-label" for="${chartType}_response_markers">
                  <span style="color: #ffc0cb;">▲</span><span style="color: #ff69b4;">○</span><span style="color: #c71585;">◆</span> <strong>反応速度マーカー（ピンク系）を表示</strong>
                </label>
              </div>
            </div>
            <button class="btn btn-danger btn-sm" onclick="deleteSelectedSessions('${chartType}')">
              <i class="bi bi-trash"></i> 選択したセッションを削除
            </button>
          </div>
        </div>
      </div>
    </div>
  `;
  
  // セッションリストを追加
  html += datasets.map((dataset, index) => `
    <div class="col-md-4 col-sm-6 mb-2">
      <div class="form-check">
        <input class="form-check-input" type="checkbox" id="${chartType}_session_${dataset.session_id}" 
               checked onchange="toggleSession('${chartType}', ${dataset.session_id})">
        <label class="form-check-label" for="${chartType}_session_${dataset.session_id}" style="font-size: 0.9em;">
          ${dataset.label}
        </label>
      </div>
    </div>
  `).join('');
  
  container.innerHTML = html;
}

// 元のデータを保存する変数（フィルタリング用）
let originalScoreTimelineDatasets = null;

// video_time=0のデータポイントを非表示にするトグル関数
function toggleVideoTime0Filter() {
  const checkbox = document.getElementById('hideVideoTime0');
  const chart = chartInstances.scoreTimeline;
  
  if (!chart || !originalScoreTimelineDatasets) return;
  
  // 各データセットをフィルタリング
  chart.data.datasets.forEach((dataset, index) => {
    if (originalScoreTimelineDatasets[index]) {
      if (checkbox.checked) {
        // video_time が 1秒以下のポイントを除外
        dataset.data = originalScoreTimelineDatasets[index].data.filter(point => {
          // yプロパティ（video_time）が1秒より大きいもののみ表示
          return point.y > 1;
        });
      } else {
        // 元のデータを復元
        dataset.data = [...originalScoreTimelineDatasets[index].data];
      }
    }
  });
  
  chart.update();
}

function initializeAnalyticsCharts() {
  // Chart.jsが読み込まれているか確認
  if (typeof Chart === 'undefined') {
    console.log('Chart.js is not loaded yet. Retrying...');
    setTimeout(initializeAnalyticsCharts, 100);
    return;
  }
  
  // canvas要素が存在するか確認
  const scoreTimelineCtx = document.getElementById('scoreTimelineChart');
  if (!scoreTimelineCtx) {
    console.log('Canvas elements not ready yet. Retrying...');
    setTimeout(initializeAnalyticsCharts, 100);
    return;
  }
  
  // 既存のチャートを破棄
  Object.keys(chartInstances).forEach(key => {
    if (chartInstances[key]) {
      chartInstances[key].destroy();
      chartInstances[key] = null;
    }
  });
  
  // 共通のChart.jsオプション
  const commonOptions = {
    responsive: true,
    maintainAspectRatio: false,
    plugins: {
      legend: {
        display: true,
        position: 'top'
      }
    }
  };

  // 1. 集中スコア推移グラフ
  if (scoreTimelineCtx) {
    const scoreTimelineData = <%= raw @score_timeline_data.to_json %>;
    const videoOperationsMarkers = <%= raw @video_operations_markers.to_json %>;
    const responseTimeMarkers = <%= raw @response_time_markers.to_json %>;
    
    // デバッグ: 反応速度マーカーのデータを確認
    console.log('[反応速度デバッグ] responseTimeMarkers:', responseTimeMarkers);
    console.log('[反応速度デバッグ] マーカー数:', responseTimeMarkers.length);
    responseTimeMarkers.forEach((marker, index) => {
      console.log(`[反応速度デバッグ] セッション ${marker.session_id}:`, {
        quick: marker.quick?.length || 0,
        normal: marker.normal?.length || 0,
        slow: marker.slow?.length || 0,
        data: marker
      });
    });
    
    // 線の色をグレーに統一
    const lineColor = 'rgba(128, 128, 128, 0.5)'; // グレー
    const operationColor = '#6f42c1'; // 紫色
    const responseQuickColor = '#ff65B2'; // 薄いピンク色（素早い回答用）
    const responseNormalColor = '#ff007F'; // ピンク色（普通の回答用）
    const responseSlowColor = '#98004c'; // 薄暗いピンク色（遅延用）
    const quickCheckColor = '#17a2b8'; // 水色（速い問題確認用）

    // 集中度スコアで色を決定する関数
    const getColorByScore = (score) => {
      if (score <= 25) return '#dc3545'; // 赤
      if (score <= 50) return '#fd7e14'; // オレンジ
      if (score <= 75) return '#ffc107'; // 黄色
      return '#28a745'; // 緑
    };

    // データセットを作成（セッションごとにグループ化）
    const datasets = [];
    const sessionDatasetGroups = {}; // セッションIDごとのデータセットインデックスを記録
    
    // 各セッションのデータを追加
    scoreTimelineData.forEach((session, sessionIndex) => {
      const sessionData = session.data.map(point => ({
        x: point.x,
        y: point.video_time || 0,
        score: point.y,
        video_time: point.video_time,
        color: getColorByScore(point.y)
      }));
      
      // 集中スコアラインを追加
      const lineDatasetIndex = datasets.length;
      datasets.push({
        label: session.label,
        data: sessionData,
        borderColor: lineColor,
        borderWidth: 2,
        showLine: true,
        tension: 0,
        pointRadius: 1,
        pointHoverRadius: 5,
        pointBackgroundColor: sessionData.map(d => d.color),
        pointBorderColor: sessionData.map(d => d.color),
        order: 2,
        sessionId: session.session_id
      });
      
      // このセッションに属するデータセットのインデックスを記録
      sessionDatasetGroups[session.session_id] = [lineDatasetIndex];
      
      // 対応する動画操作マーカーをまとめて追加
      const ops = videoOperationsMarkers.find(o => o.session_id === session.session_id);
      if (ops) {
        // すべての操作マーカーを1つの配列にまとめる
        const allOperations = [];
        
        // スキップ/シークマーカー（三角形）
        if (ops.skip && ops.skip.length > 0) {
          ops.skip.forEach(point => {
            allOperations.push({ ...point, style: 'triangle', type: 'スキップ' });
          });
        }
        
        // 一時停止マーカー（四角形）
        if (ops.pause && ops.pause.length > 0) {
          ops.pause.forEach(point => {
            allOperations.push({ ...point, style: 'rect', type: '一時停止' });
          });
        }
        
        // その他の操作マーカー（円形）
        if (ops.other && ops.other.length > 0) {
          ops.other.forEach(point => {
            allOperations.push({ ...point, style: 'circle', type: 'その他' });
          });
        }
        
        // すべての操作を1つのデータセットとして追加
        if (allOperations.length > 0) {
          sessionDatasetGroups[session.session_id].push(datasets.length);
          datasets.push({
            label: `${ops.label} - 操作`,
            data: allOperations,
            backgroundColor: operationColor,
            borderColor: operationColor,
            pointRadius: 3,
            pointHoverRadius: 5,
            pointStyle: function(context) {
              const point = context.raw;
              return point.style || 'circle';
            },
            showLine: false,
            order: 1,
            sessionId: session.session_id
          });
        }
      }
      
      // 反応速度マーカーを追加
      const resp = responseTimeMarkers.find(r => r.session_id === session.session_id);
      console.log(`[反応速度デバッグ] セッション ${session.session_id} のマーカー検索結果:`, resp);
      if (resp) {
        const allResponses = [];
        
        // 素早い回答マーカー（上向き三角▲、薄いピンク）
        if (resp.quick && resp.quick.length > 0) {
          console.log(`[反応速度デバッグ] セッション ${session.session_id}: quick=${resp.quick.length}件追加`);
          resp.quick.forEach(point => {
            allResponses.push({ ...point, style: 'triangle', type: '素早い回答', color: responseQuickColor });
          });
        }
        
        // 通常の回答速度マーカー（円形○、ピンク）
        if (resp.normal && resp.normal.length > 0) {
          console.log(`[反応速度デバッグ] セッション ${session.session_id}: normal=${resp.normal.length}件追加`);
          resp.normal.forEach(point => {
            allResponses.push({ ...point, style: 'circle', type: '回答速度普通', color: responseNormalColor });
          });
        }
        
        // 回答遅延マーカー（下向き三角▼、薄暗いピンク）
        if (resp.slow && resp.slow.length > 0) {
          console.log(`[反応速度デバッグ] セッション ${session.session_id}: slow=${resp.slow.length}件追加`);
          resp.slow.forEach(point => {
            allResponses.push({ ...point, style: 'rectRot', type: '回答遅延', color: responseSlowColor });
          });
        }
        
        // 速い問題確認マーカー（星型★、水色）
        if (resp.quick_check && resp.quick_check.length > 0) {
          console.log(`[速い問題確認デバッグ] セッション ${session.session_id}: quick_check=${resp.quick_check.length}件追加`);
          resp.quick_check.forEach(point => {
            allResponses.push({ ...point, style: 'star', type: '速い問題確認', color: quickCheckColor });
          });
        }
        
        console.log(`[反応速度デバッグ] セッション ${session.session_id}: 合計 ${allResponses.length}件の反応速度マーカー`)
        
        if (allResponses.length > 0) {
          console.log(`[反応速度デバッグ] セッション ${session.session_id}: データセット追加 (${allResponses.length}件)`, allResponses);
          sessionDatasetGroups[session.session_id].push(datasets.length);
          const responseDataset = {
            label: `${resp.label} - 反応速度`,
            data: allResponses,
            pointRadius: 9,
            pointHoverRadius: 11,
            pointStyle: function(context) {
              const point = context.raw;
              return point.style || 'circle';
            },
            pointBackgroundColor: function(context) {
              const point = context.raw;
              return point.color || responseNormalColor;
            },
            pointBorderColor: function(context) {
              const point = context.raw;
              return point.color || responseNormalColor;
            },
            showLine: false,
            order: 1,
            sessionId: session.session_id
          };
          datasets.push(responseDataset);
          console.log(`[反応速度デバッグ] データセット追加完了:`, responseDataset);
        } else {
          console.log(`[反応速度デバッグ] セッション ${session.session_id}: マーカーが0件のためスキップ`);
        }
      } else {
        console.log(`[反応速度デバッグ] セッション ${session.session_id}: マーカーデータが見つかりません`);
      }
    });

    // オリジナルデータを深いコピーで保存（フィルタリング前の状態）
    originalScoreTimelineDatasets = JSON.parse(JSON.stringify(datasets));
    
    chartInstances.scoreTimeline = new Chart(scoreTimelineCtx, {
      type: 'scatter',
      data: { datasets },
      options: {
        ...commonOptions,
        scales: {
          x: {
            type: 'linear',
            title: {
              display: true,
              text: 'セッション経過時間（秒）'
            },
            ticks: {
              callback: function(value) {
                const minutes = Math.floor(value / 60);
                const seconds = Math.floor(value % 60);
                return `${minutes}:${seconds.toString().padStart(2, '0')}`;
              }
            }
          },
          y: {
            type: 'linear',
            title: {
              display: true,
              text: '動画再生時間（秒）'
            },
            ticks: {
              callback: function(value) {
                const minutes = Math.floor(value / 60);
                const seconds = Math.floor(value % 60);
                return `${minutes}:${seconds.toString().padStart(2, '0')}`;
              }
            },
            beginAtZero: true
          }
        },
        plugins: {
          ...commonOptions.plugins,
          zoom: {
            zoom: {
              wheel: {
                enabled: true,
              },
              pinch: {
                enabled: true
              },
              mode: 'xy',
            },
            pan: {
              enabled: true,
              mode: 'xy',
            }
          },
          tooltip: {
            callbacks: {
              label: function(context) {
                const point = context.raw;
                const videoMinutes = Math.floor(context.parsed.y / 60);
                const videoSeconds = Math.floor(context.parsed.y % 60);
                
                // 動画操作マーカーの場合
                if (context.dataset.label.includes('操作')) {
                  const operationType = point.type || '操作';
                  return `${operationType} | 動画: ${videoMinutes}:${videoSeconds.toString().padStart(2, '0')}${point.description ? ' - ' + point.description : ''}`;
                }
                
                // 反応速度マーカーの場合
                if (context.dataset.label.includes('反応速度')) {
                  const responseType = point.type || '反応';
                  
                  // 速い問題確認の場合は視線速度データを表示
                  if (point.type === '速い問題確認' && point.details) {
                    const gazeVelocityX = point.details.gazeVelocityX || 'N/A';
                    const gazeVelocityY = point.details.gazeVelocityY || 'N/A';
                    const threshold = point.details.threshold || 5;
                    const timeFromQuestion = point.details.timeFromQuestion || 'N/A';
                    return [
                      `${responseType} | 動画: ${videoMinutes}:${videoSeconds.toString().padStart(2, '0')}`,
                      `視線速度X: ${gazeVelocityX} px/s (閾値: ${threshold} px/s)`,
                      `視線速度Y: ${gazeVelocityY} px/s`,
                      `問題表示から: ${timeFromQuestion} 秒`
                    ];
                  }
                  
                  // その他の反応速度マーカー
                  const responseTime = point.response_time ? ` (${point.response_time.toFixed(1)}秒)` : '';
                  return `${responseType}${responseTime} | 動画: ${videoMinutes}:${videoSeconds.toString().padStart(2, '0')}${point.description ? ' - ' + point.description : ''}`;
                }
                
                // 集中スコアラインの場合
                const score = point.score !== undefined ? point.score : 0;
                return `${context.dataset.label} - 集中度: ${score.toFixed(1)}点 | 動画: ${videoMinutes}:${videoSeconds.toString().padStart(2, '0')}`;
              },
              title: function(context) {
                const minutes = Math.floor(context[0].parsed.x / 60);
                const seconds = Math.floor(context[0].parsed.x % 60);
                return `経過時間: ${minutes}:${seconds.toString().padStart(2, '0')}`;
              }
            }
          }
        }
      }
    });
    
    // マーカーホバー時に同じセッションのラインの色を変更
    let currentHoveredSessionId = null;
    
    chartInstances.scoreTimeline.options.onHover = function(event, activeElements) {
      if (activeElements.length > 0) {
        const hoveredDatasetIndex = activeElements[0].datasetIndex;
        const hoveredDataset = this.data.datasets[hoveredDatasetIndex];
        const hoveredSessionId = hoveredDataset.sessionId;
        
        // 既に同じセッションをホバー中なら何もしない
        if (currentHoveredSessionId === hoveredSessionId) {
          return;
        }
        
        currentHoveredSessionId = hoveredSessionId;
        
        // すべてのラインを元の色に戻してから、ホバー中のセッションのラインを青色に
        this.data.datasets.forEach((dataset) => {
          if (dataset.showLine) {
            if (dataset.sessionId === hoveredSessionId) {
              dataset.borderColor = 'rgba(54, 162, 235, 0.8)'; // 青色
              dataset.borderWidth = 3;
            } else {
              dataset.borderColor = lineColor;
              dataset.borderWidth = 2;
            }
          }
        });
        
        this.update('none'); // アニメーションなしで更新
      } else {
        // ホバーが外れた場合は元の色に戻す
        if (currentHoveredSessionId !== null) {
          currentHoveredSessionId = null;
          
          this.data.datasets.forEach((dataset) => {
            if (dataset.showLine) {
              dataset.borderColor = lineColor;
              dataset.borderWidth = 2;
            }
          });
          
          this.update('none');
        }
      }
    };
    
    // セッション切り替えトグルを作成
    createSessionToggles('scoreTimeline', scoreTimelineData);
    
    // ダブルクリックでズームリセット
    scoreTimelineCtx.ondblclick = function() {
      chartInstances.scoreTimeline.resetZoom();
    };
  }

  // 2. イベント分布グラフ（円グラフ）
  const eventDistCtx = document.getElementById('eventDistributionChart');
  if (eventDistCtx) {
    const eventData = <%= raw @event_distribution_data.to_json %>;
    
    chartInstances.eventDistribution = new Chart(eventDistCtx, {
      type: 'doughnut',
      data: {
        labels: eventData.labels,
        datasets: [{
          data: eventData.values,
          backgroundColor: Object.values(eventData.colors),
          borderWidth: 2,
          borderColor: '#fff'
        }]
      },
      options: {
        ...commonOptions,
        plugins: {
          ...commonOptions.plugins,
          legend: {
            position: 'right'
          },
          tooltip: {
            callbacks: {
              label: function(context) {
                const total = context.dataset.data.reduce((a, b) => a + b, 0);
                const percentage = ((context.parsed / total) * 100).toFixed(1);
                return context.label + ': ' + context.parsed + '件 (' + percentage + '%)';
              }
            }
          }
        }
      }
    });
  }

  // 3. 時間帯別活動量グラフ
  const hourlyCtx = document.getElementById('hourlyActivityChart');
  if (hourlyCtx) {
    const hourlyData = <%= raw @hourly_activity_data.to_json %>;
    
    chartInstances.hourlyActivity = new Chart(hourlyCtx, {
      type: 'bar',
      data: {
        labels: hourlyData.labels.map(h => h + '時'),
        datasets: [{
          label: 'イベント数',
          data: hourlyData.values,
          backgroundColor: 'rgba(54, 162, 235, 0.8)',
          borderColor: 'rgba(54, 162, 235, 1)',
          borderWidth: 1
        }]
      },
      options: {
        ...commonOptions,
        scales: {
          x: {
            title: {
              display: true,
              text: '時間帯'
            }
          },
          y: {
            title: {
              display: true,
              text: 'イベント数'
            },
            beginAtZero: true,
            ticks: {
              stepSize: 1
            }
          }
        },
        plugins: {
          ...commonOptions.plugins,
          zoom: {
            zoom: {
              wheel: {
                enabled: true,
              },
              pinch: {
                enabled: true
              },
              mode: 'xy',
            },
            pan: {
              enabled: true,
              mode: 'xy',
            }
          }
        }
      }
    });
    
    // ダブルクリックでズームリセット
    hourlyCtx.ondblclick = function() {
      chartInstances.hourlyActivity.resetZoom();
    };
  }

  // 4. ユーザー別平均スコアグラフ
  const userScoresCtx = document.getElementById('userScoresChart');
  if (userScoresCtx) {
    const userData = <%= raw @user_score_data.to_json %>;
    
    chartInstances.userScores = new Chart(userScoresCtx, {
      type: 'bar',
      data: {
        labels: userData.labels,
        datasets: [{
          label: '平均スコア',
          data: userData.values,
          backgroundColor: userData.values.map(score => {
            if (score >= 70) return 'rgba(40, 167, 69, 0.8)';
            if (score >= 50) return 'rgba(255, 193, 7, 0.8)';
            return 'rgba(220, 53, 69, 0.8)';
          }),
          borderWidth: 1
        }]
      },
      options: {
        indexAxis: 'y',
        ...commonOptions,
        scales: {
          x: {
            title: {
              display: true,
              text: '平均集中スコア'
            },
            beginAtZero: true,
            max: 100
          },
          y: {
            title: {
              display: true,
              text: 'ユーザー'
            }
          }
        },
        plugins: {
          ...commonOptions.plugins,
          tooltip: {
            callbacks: {
              label: function(context) {
                const index = context.dataIndex;
                const score = context.parsed.x.toFixed(1);
                const sessions = userData.counts[index];
                return [
                  '平均スコア: ' + score + '点',
                  'セッション数: ' + sessions + '回'
                ];
              }
            }
          }
        }
      }
    });
  }

  // 5. 動画時間別イベント密度グラフ
  const videoDensityCtx = document.getElementById('videoDensityChart');
  if (videoDensityCtx) {
    const densityData = <%= raw @video_time_density_data.to_json %>;
    
    chartInstances.videoDensity = new Chart(videoDensityCtx, {
      type: 'bar',
      data: {
        labels: densityData.map(d => d.label),
        datasets: [{
          label: 'イベント数',
          data: densityData.map(d => d.y),
          backgroundColor: 'rgba(255, 99, 132, 0.8)',
          borderColor: 'rgba(255, 99, 132, 1)',
          borderWidth: 1
        }]
      },
      options: {
        ...commonOptions,
        scales: {
          x: {
            title: {
              display: true,
              text: '動画時間（30秒単位）'
            }
          },
          y: {
            title: {
              display: true,
              text: 'イベント数'
            },
            beginAtZero: true,
            ticks: {
              stepSize: 1
            }
          }
        },
        plugins: {
          ...commonOptions.plugins,
          zoom: {
            zoom: {
              wheel: {
                enabled: true,
              },
              pinch: {
                enabled: true
              },
              mode: 'xy',
            },
            pan: {
              enabled: true,
              mode: 'xy',
            }
          },
          tooltip: {
            callbacks: {
              label: function(context) {
                return 'イベント数: ' + context.parsed.y + '件';
              }
            }
          }
        }
      }
    });
    
    // ダブルクリックでズームリセット
    videoDensityCtx.ondblclick = function() {
      chartInstances.videoDensity.resetZoom();
    };
  }

  // 6. 動画操作マップ（全体）
  const videoOperationsCtx = document.getElementById('videoOperationsChart');
  if (videoOperationsCtx) {
    const operationsData = <%= raw @video_operations_map_data.to_json %>;
    
    chartInstances.videoOperations = new Chart(videoOperationsCtx, {
      type: 'bar',
      data: {
        labels: operationsData.map(d => d.label),
        datasets: [
          {
            label: '一時停止',
            data: operationsData.map(d => d.pause),
            backgroundColor: 'rgba(255, 193, 7, 0.8)',
            borderColor: 'rgba(255, 193, 7, 1)',
            borderWidth: 1
          },
          {
            label: 'スキップ/シーク',
            data: operationsData.map(d => d.seek),
            backgroundColor: 'rgba(220, 53, 69, 0.8)',
            borderColor: 'rgba(220, 53, 69, 1)',
            borderWidth: 1
          }
        ]
      },
      options: {
        ...commonOptions,
        scales: {
          x: {
            title: {
              display: true,
              text: '動画時間'
            },
            ticks: {
              maxRotation: 45,
              minRotation: 45
            }
          },
          y: {
            title: {
              display: true,
              text: '操作回数'
            },
            beginAtZero: true,
            ticks: {
              stepSize: 1
            }
          }
        },
        plugins: {
          ...commonOptions.plugins,
          zoom: {
            zoom: {
              wheel: {
                enabled: true,
              },
              pinch: {
                enabled: true
              },
              mode: 'xy',
            },
            pan: {
              enabled: true,
              mode: 'xy',
            }
          },
          tooltip: {
            callbacks: {
              label: function(context) {
                return context.dataset.label + ': ' + context.parsed.y + '回';
              },
              footer: function(context) {
                const index = context[0].dataIndex;
                const total = operationsData[index].total;
                return '合計: ' + total + '回';
              }
            }
          }
        }
      }
    });
    
    videoOperationsCtx.ondblclick = function() {
      chartInstances.videoOperations.resetZoom();
    };
  }

  // 7. ユーザー別動画操作
  const userOperationsCtx = document.getElementById('userOperationsChart');
  if (userOperationsCtx) {
    const userOpsData = <%= raw @user_operations_map_data.to_json %>;
    
    const colors = [
      'rgba(255, 99, 132, 0.6)',
      'rgba(54, 162, 235, 0.6)',
      'rgba(255, 206, 86, 0.6)',
      'rgba(75, 192, 192, 0.6)',
      'rgba(153, 102, 255, 0.6)',
      'rgba(255, 159, 64, 0.6)',
      'rgba(199, 199, 199, 0.6)',
      'rgba(83, 102, 255, 0.6)'
    ];
    
    chartInstances.userOperations = new Chart(userOperationsCtx, {
      type: 'scatter',
      data: {
        datasets: userOpsData.map((session, index) => ({
          label: session.label,
          data: session.data,
          backgroundColor: colors[index % colors.length],
          borderColor: colors[index % colors.length].replace('0.6', '1'),
          pointRadius: function(context) {
            const point = context.raw;
            return 3 + (point.y * 2); // 操作回数に応じてポイントサイズを変更
          },
          pointHoverRadius: function(context) {
            const point = context.raw;
            return 5 + (point.y * 2);
          }
        }))
      },
      options: {
        ...commonOptions,
        scales: {
          x: {
            type: 'linear',
            title: {
              display: true,
              text: '動画時間（秒）'
            },
            ticks: {
              callback: function(value) {
                const minutes = Math.floor(value / 60);
                const seconds = value % 60;
                return minutes + ':' + seconds.toString().padStart(2, '0');
              }
            }
          },
          y: {
            title: {
              display: true,
              text: '操作回数'
            },
            beginAtZero: true,
            ticks: {
              stepSize: 1
            }
          }
        },
        plugins: {
          ...commonOptions.plugins,
          zoom: {
            zoom: {
              wheel: {
                enabled: true,
              },
              pinch: {
                enabled: true
              },
              mode: 'xy',
            },
            pan: {
              enabled: true,
              mode: 'xy',
            }
          },
          tooltip: {
            callbacks: {
              label: function(context) {
                const point = context.raw;
                const minutes = Math.floor(point.x / 60);
                const seconds = Math.floor(point.x % 60);
                return [
                  context.dataset.label,
                  `動画時間: ${minutes}:${seconds.toString().padStart(2, '0')}`,
                  `操作回数: ${point.y}回`,
                  `  一時停止: ${point.pause}回`,
                  `  スキップ: ${point.seek}回`
                ];
              }
            }
          }
        }
      }
    });
    
    // セッション切り替えトグルを作成
    createSessionToggles('userOperations', userOpsData);
    
    userOperationsCtx.ondblclick = function() {
      chartInstances.userOperations.resetZoom();
    };
  }
}

// 初回読み込みとTurbo遷移の両方で実行
// DOMContentLoadedは通常のページ読み込み用
document.addEventListener('DOMContentLoaded', function() {
  // ページが完全に読み込まれるまで少し待機
  setTimeout(initializeAnalyticsCharts, 50);
});

// turbo:loadはTurbo遷移時用
document.addEventListener('turbo:load', function() {
  // Turbo遷移後も少し待機してからチャートを初期化
  setTimeout(initializeAnalyticsCharts, 50);
});

// turbo:renderはTurbo遷移のレンダリング完了時用（より確実）
document.addEventListener('turbo:render', function() {
  setTimeout(initializeAnalyticsCharts, 50);
});

// Turbo遷移前にチャートをクリーンアップ
document.addEventListener('turbo:before-render', function() {
  // 保持しているチャートインスタンスを破棄
  Object.keys(chartInstances).forEach(key => {
    if (chartInstances[key]) {
      chartInstances[key].destroy();
      chartInstances[key] = null;
    }
  });
});
<% end %>
</script>
